<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="../stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>

</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input  name="title"  class="block px-5 py-2 bg-zinc-800 w-full rounded-md" type="text" placeholder="enter the value">
                <textarea name="details"  class="block px-5 py-2 bg-zinc-900  mt-2 w-full rounded-md resize-none"
                  placeholder="right your task details here"></textarea>
                <input class="bg-blue-700 text-black mt-6 px-4 py-4 rounded-full text-2xl" type="submit"
                    value="create task">
            </form>

        </div>
        <div class="tasks p-10  flex gap-4 flex-wrap">
            <% if(files.length > 0){ %>
                <% files.forEach(function(val){ %>
                    <div class="task w-72  px-3 py-4 bg-zinc-800 rounded-md">
                        <h1 class="text-2xl text-white tracking-tight"><%= val%></h1>
                        <a class="bg-blue-500 inline-block mt-4 rounded-md p-2" href="/file/<%= val%>">Read more</a>
                    </div>
                    <div class="task w-72 hidden px-3 py-4 bg-zinc-800 rounded-md">
                        <h1 class="text-2xl text-white tracking-tight">some task goes here</h1>
                        <a class="bg-blue-500 inline-block mt-4 rounded-md p-2" href="">Read more</a>
                    </div>
                    <div class="task w-72 hidden  px-3 py-4 bg-zinc-800 rounded-md">
                        <h1 class="text-2xl text-white tracking-tight">some task goes here</h1>
                        <a class="bg-blue-500 inline-block mt-4 rounded-md p-2" href="">Read more</a>
                    </div>

                    <% })%>

                        <% } else { %>
                            <h3 class="bg-zinc-600 p-3 text-xl">No task yet</h3>

                            <% }%>

        </div>

    </div>



</body>

</html>